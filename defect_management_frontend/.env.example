# Backend API base URL (public; embedded into the client bundle)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Supabase (public; embedded into the client bundle)
# Placeholder-friendly for builds, but required for auth flows at runtime.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
